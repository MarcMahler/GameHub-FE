{
  "name": "gamehub-be",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "node scripts/seedDatabase.js",
    "db:manage": "node scripts/dbManager.js",
    "db:clear": "node -e \"const DB = require('./scripts/dbManager'); new DB().clearDatabase().then(() => process.exit())\"",
    "db:inspect": "node scripts/inspectDatabase.js"
  },
  "keywords": ["gamehub", "games", "backend", "api"],
  "author": "",
  "license": "ISC",
  "description": "Backend API for GameHub - A platform for simple games like tic-tac-toe, chess, etc.",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongodb": "^6.20.0",
    "mongoose": "^8.18.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
